将以下章节内容按场景切分。场景以地点或时间段变化为界，每个场景对应一个完整情节片段。

以 JSON 数组输出，每个场景包含：
- location: 地点（10字以内，如"客栈大堂"）
- timeDesc: 时间（如"白天"/"夜晚"/"黄昏"）
- isInterior: 是否室内（true/false）
- characters: 涉及角色名（JSON 字符串数组，如 ["张三","李四"]）
- sourceContent: 该场景对应的原文片段（完整引用，保持原文）

按原文顺序输出，不遗漏任何情节。只输出 JSON 数组，不要解释。

章节标题：{{chapterTitle}}
章节内容：
{{content}}
